# 서빙에만 필요한 것. 학습용(optuna, torch, playwright 등)은 넣지 않습니다.
#
# 버전은 학습 환경과 맞춰야 합니다. 여기가 어긋나면 배포가 성공해도 예측값이
# 달라집니다. 실제로 xgboost를 2.1.3으로 두었더니 3.2.0으로 학습한 모델에서
# 같은 입력에 111.0 대신 -44.2가 나왔습니다. 범주형 분기 처리가 판본 사이에
# 달라지기 때문입니다.
fastapi==0.140.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
xgboost==3.2.0
pandas==3.0.5
numpy==2.4.6
pyarrow==24.0.0
scipy==1.17.1
requests==2.34.2
openpyxl==3.1.5
